# Scope

- This is a Next.js (App Router) + TypeScript project.
- UI uses shadcn/ui + Tailwind.
- Prefer minimal dependencies.
- Prefer server components by default; use client components only when needed.